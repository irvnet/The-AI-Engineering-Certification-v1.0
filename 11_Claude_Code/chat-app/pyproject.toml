[project]
name = "chat-app"
version = "0.1.0"
description = "Skeleton chat web app: FastAPI backend + plain HTML/CSS/JS UI"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.2.123",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]
